"use client";

import { AppBar, Toolbar } from "@mui/material";

export const Header = () => {
  return (
    <AppBar
      elevation={0}
      position="static"
      sx={{}}
    >
      <Toolbar>
        header
      </Toolbar>
    </AppBar>
  );
};
