"use client";

import { Layout } from "@/ui/modules/partials";
import { BlockchainSearchSuccess } from "./ui/component";

export const BlockchainSucess = () => {
  return (
    <Layout>
      <BlockchainSearchSuccess />
    </Layout>
  );
};
